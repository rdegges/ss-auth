extends base

block vars
  - var title = "Login"

block body
  .container
    .vertical-center
      .jumbotron.col-sm-8
        h3 Please sign in below.
        if error
          p.error #{error}
        form.form-horizontal(method="post")
          input(type="hidden", name="_csrf", value=csrfToken)
          .form-group
            label.col-sm-2.control-label(for="email") Email
            .col-sm-10
              input.form-control(type="email", id="email", name="email", placeholder="Email", required=true, autofocus=true)
          .form-group
            label.col-sm-2.control-label(for="password") Password
            .col-sm-10
              input.form-control(type="password", id="password", name="password", placeholder="Password", required=true)
          .form-group
            .col-sm-offset-2.col-sm-10
              button.btn.btn-default(type="submit") Sign in
