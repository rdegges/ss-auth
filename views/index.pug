extends base

block vars
  - var title = "Home"

block body
  .container.home
    .vertical-center
      .jumbotron
        p.
          Please #[a(href="/register") register] or #[a(href="/login") login]
          to continue!
